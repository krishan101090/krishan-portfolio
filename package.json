{
  "name": "krishan-portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rm -rf .next && next dev -p 3000",
    "dev:fast": "next dev -p 3000",
    "dev:clean": "rm -rf .next && next dev -p 3000",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "^14.2.15",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "eslint-config-next": "^14.2.15"
  }
}
